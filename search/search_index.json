{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"cc-logger","text":"<p>A Python-logging-style logger for CC: Tweaked, written in pure Lua.  Install it with a single <code>wget</code>, then use <code>logger.new()</code> to get a Logger that sends formatted messages to the terminal, files, modems, websockets, or  custom handlers.</p>"},{"location":"#level-hierarchy","title":"Level Hierarchy","text":"Level Weight Constant <code>DEBUG</code> 1 <code>logger.DEBUG</code> <code>INFO</code> 2 <code>logger.INFO</code> <code>WARNING</code> 3 <code>logger.WARNING</code> <code>ERROR</code> 4 <code>logger.ERROR</code> <code>CRITICAL</code> 5 <code>logger.CRITICAL</code> <p>Messages below the logger's current level are dropped silently. Default level is <code>INFO</code>.</p>"},{"location":"#install","title":"Install","text":"<pre><code>wget https://raw.githubusercontent.com/SethGamer1223/cc-logger/main/logger.lua\n</code></pre>"},{"location":"#at-a-glance","title":"At a Glance","text":"<pre><code>local logger = require(\"logger.lua\")\n\nlocal log = logger.new(\"myApp\")\nlog:info(\"System started\")\nlog:warn(\"Disk space low\")\nlog:error(\"Something broke\")\n</code></pre>"},{"location":"#next-steps","title":"Next Steps","text":"<ul> <li>Quick Start first logger </li> <li>Architecture Overview how logger, handler, and formatter fit together</li> <li>API Reference every function documented</li> <li>Cookbook</li> </ul>"},{"location":"#license","title":"License","text":"<p>MIT  see LICENSE.</p>"},{"location":"known-quirks/","title":"Known Quirks &amp; Limitations","text":"<p>This page documents real behaviors found in the code that may be unexpected</p>"},{"location":"known-quirks/#default-level-is-info-not-configurable-at-construction","title":"Default Level Is <code>INFO</code>, Not Configurable at Construction","text":"<p>There is no way to set the initial level when calling <code>logger.new()</code>. The level is always <code>logger.INFO</code>. To change it, you must call <code>:setLevel()</code> afterward:</p> <pre><code>local log = logger.new(\"app\")\nlog:setLevel(logger.DEBUG)  -- explicit second step\n</code></pre>"},{"location":"known-quirks/#rotatingfilehandler-tracks-length-in-memory","title":"<code>RotatingFileHandler</code> Tracks Length In-Memory","text":"<p>The byte counter <code>self.length</code> is maintained in memory and seeded from the file's <code>seek(\"end\")</code> on construction. If another process writes to the same file, the in-memory counter drifts from the real file size.</p>"},{"location":"quick-start/","title":"Quick Start","text":"<p>Get a working logger in under two minutes.</p>"},{"location":"quick-start/#1-install","title":"1. Install","text":"<p>On your ComputerCraft computer, run:</p> <pre><code>wget https://raw.githubusercontent.com/SethGamer1223/cc-logger/main/logger.lua\n</code></pre>"},{"location":"quick-start/#2-minimal-logger","title":"2. Minimal Logger","text":"<p>Create a file <code>test-logger.lua</code>:</p> <pre><code>local logger = require(\"logger.lua\")\n\n-- Create a logger named test\nlocal log = logger.new(\"test\")\n\n-- Log messages at each level\nlog:debug(\"Debug info\")       -- doesnt show (default logging level is INFO)\nlog:info(\"System ready\")\nlog:warn(\"Low memory\")\nlog:error(\"Disk write failed\")\nlog:critical(\"REACTOR MELTDOWN\")\n</code></pre> <p>outputs:</p> <pre><code>[test] 2026/07/06 14:30:01 [INFO] System ready\n[test] 2026/07/06 14:30:01 [WARNING] Low memory\n[test] 2026/07/06 14:30:01 [ERROR] Disk write failed\n[test] 2026/07/06 14:30:01 [CRITICAL] REACTOR MELTDOWN\n</code></pre> <p>The <code>DEBUG</code> line is silent because the default minimum level is <code>INFO</code>. The default Formatter produces the <code>[name] timestamp [LEVEL] message</code> pattern above.</p>"},{"location":"quick-start/#3-whats-next","title":"3. What's Next","text":"<ul> <li>Architecture Overview understand Logger \u2192 Handler \u2192 Formatter</li> <li>Log Levels <code>setLevel</code>, <code>isEnabledFor</code>, and numeric weights</li> <li>Handlers  send logs to files, modems, websockets, and more</li> </ul>"},{"location":"api/logger/","title":"Logger API Reference","text":"<p>A <code>Logger</code> object is created with <code>logger.new()</code>. It holds a name, a minimum level, and a list of handlers. Every method below is available on the returned instance.</p>"},{"location":"api/logger/#loggernewname-removedefaulthandle-new","title":"<code>logger.new(name, RemoveDefaultHandle)</code> {: #new}","text":"<p>Creates a new Logger instance.</p> Parameter Type Default Description <code>name</code> <code>string</code>|<code>nil</code> <code>\"root\"</code> Used in <code>{loggername}</code> token substitution <code>RemoveDefaultHandle</code> <code>boolean</code>|<code>nil</code> <code>false</code> If <code>true</code>, skip attaching default <code>TerminalHandler</code> <p>The default level is <code>logger.INFO</code>.</p> <pre><code>local logger = require(\"logger.lua\")\n\n-- With default TerminalHandler\nlocal log1 = logger.new(\"app\")\n\n-- No handlers at all\nlocal log2 = logger.new(\"noHandlers\", true)\n</code></pre>"},{"location":"api/logger/#setlevellevel-setlevel","title":"<code>:setLevel(level)</code> {: #setlevel}","text":"<p>Set the minimum logging threshold. Returns <code>true</code> if the level was valid, <code>false</code> otherwise.</p> Parameter Type Description <code>level</code> <code>table</code> A level table (e.g. <code>logger.WARNING</code>) <pre><code>local log = logger.new(\"demo\")\nlocal ok = log:setLevel(logger.WARNING)\nprint(ok)  -- true\nlog:setLevel({99, \"NONSENSE\"})  -- the level is valid (any {number, string} table works)\n</code></pre>"},{"location":"api/logger/#isenabledforlevel-isenabledfor","title":"<code>:isEnabledFor(level)</code> {: #isenabledfor}","text":"<p>Check whether a message at the given level would be logged (without actually logging it).</p> Parameter Type Description <code>level</code> <code>table</code> Level table to check <p>Returns <code>true</code> when <code>level[1] &gt;= self.level[1]</code> and the level is valid.</p> <pre><code>local log = logger.new(\"checker\")\nlog:setLevel(logger.WARNING)\n\nprint(log:isEnabledFor(logger.DEBUG))    -- false (1 &lt; 3)\nprint(log:isEnabledFor(logger.WARNING))  -- true  (3 &gt;= 3)\nprint(log:isEnabledFor(logger.ERROR))    -- true  (4 &gt;= 3)\n</code></pre>"},{"location":"api/logger/#geteffectivelevel-geteffectivelevel","title":"<code>:getEffectiveLevel()</code> {: #geteffectivelevel}","text":"<p>Returns the current level table.</p> <pre><code>local log = logger.new(\"demo\")\nlocal lv = log:getEffectiveLevel()\nprint(lv[1])  -- 2 (INFO)\nprint(lv[2])  -- \"INFO\"\n</code></pre>"},{"location":"api/logger/#addhandlerhandler-addhandler","title":"<code>:addHandler(handler)</code> {: #addhandler}","text":"<p>Register a handler. The handler must implement <code>:handle(msg, extra, level)</code>, <code>:format(msg, extra)</code>, and <code>:addTo(logger)</code>.</p> Parameter Type Description <code>handler</code> <code>table</code> A handler instance <pre><code>local log = logger.new(\"multi\", true)\nlocal termH = logger.ColoredTerminalHandler()\nlog:addHandler(termH)\n</code></pre>"},{"location":"api/logger/#logmsg-level-extra-log","title":"<code>:log(msg, level, extra)</code> {: #log}","text":"<p>The core logging method. All other logging methods (<code>:debug</code>, <code>:info</code>, etc.) call this.</p> Parameter Type Default Description <code>msg</code> <code>string</code> (required) The log message <code>level</code> <code>table</code> (required) Level table (e.g. <code>logger.INFO</code>) <code>extra</code> <code>table</code>|<code>nil</code> <code>{}</code> Extra key-value pairs for <code>{token}</code> substitution <p>The method auto-populates these keys in <code>extra</code> before processing:</p> Key Value <code>message</code> The raw <code>msg</code> string <code>level</code> The level name (e.g. <code>\"INFO\"</code>) <code>loggername</code> The logger's name <pre><code>local log = logger.new(\"custom\")\nlog:log(\"Something happened\", logger.WARNING, {\n    component = \"reactor\",\n    temp = 873\n})\n</code></pre>"},{"location":"api/logger/#debugmsg-extra-debug","title":"<code>:debug(msg, extra)</code> {: #debug}","text":"<p>Log at <code>DEBUG</code> level. Shorthand for <code>log(msg, logger.DEBUG, extra)</code>.</p> <pre><code>log:debug(\"Entering function parseConfig\")\n</code></pre>"},{"location":"api/logger/#infomsg-extra-info","title":"<code>:info(msg, extra)</code> {: #info}","text":"<p>Log at <code>INFO</code> level.</p> <pre><code>log:info(\"Server listening on port 1234\")\n</code></pre>"},{"location":"api/logger/#warnmsg-extra-warn","title":"<code>:warn(msg, extra)</code> {: #warn}","text":"<p>Log at <code>WARNING</code> level.</p> <pre><code>log:warn(\"Fuel below 10%\")\n</code></pre>"},{"location":"api/logger/#errormsg-extra-error","title":"<code>:error(msg, extra)</code> {: #error}","text":"<p>Log at <code>ERROR</code> level.</p> <pre><code>log:error(\"Failed to open file: permission denied\")\n</code></pre>"},{"location":"api/logger/#criticalmsg-extra-critical","title":"<code>:critical(msg, extra)</code> {: #critical}","text":"<p>Log at <code>CRITICAL</code> level.</p> <pre><code>log:critical(\"Reactor temperature critical \u2013 emergency shutdown\")\n</code></pre>"},{"location":"api/logger/#basicconfig-basicconfig","title":"<code>:basicConfig(...)</code> {: #basicconfig}","text":"<p>Not functional</p> <p>This method references an undefined global <code>config</code> and will error at runtime if called. See Known Quirks.</p> <pre><code>-- This will error:\n-- log:basicConfig({ level = logger.DEBUG })\n</code></pre>"},{"location":"api/logger/#full-example-building-a-logger-from-scratch","title":"Full Example: Building a Logger from Scratch","text":"<pre><code>local logger = require(\"logger.lua\")\n\n-- 1. Create logger with no default handler\nlocal log = logger.new(\"factory\", true)\n\n-- 2. Set level\nlog:setLevel(logger.DEBUG)\n\n-- 3. Build handlers\nlocal console = logger.ColoredTerminalHandler()\n\nlocal fileFmt = logger.Formatter(\"{asctime} [{level}] ({loggername}) {message}\")\nlocal fileH = logger.RotatingFileHandler(fileFmt, \"factory.log\", 16384, 5)\n\n-- 4. Attach handlers\nlog:addHandler(console)\nlog:addHandler(fileH)\n\n-- 5. Use every level\nlog:debug(\"Initializing subsystems\")\nlog:info(\"Factory online\")\nlog:warn(\"Conveyor belt speed low\")\nlog:error(\"Robot arm fault at station 3\")\nlog:critical(\"Safety interlock triggered \u2013 full stop\")\n</code></pre>"},{"location":"api/logger/#related","title":"Related","text":"<ul> <li>Architecture  how Logger dispatches to handlers</li> <li>Log Levels <code>setLevel</code>, <code>isEnabledFor</code>, <code>getEffectiveLevel</code></li> <li>Handlers Overview  all handler types</li> </ul>"},{"location":"concepts/architecture/","title":"Architecture Overview","text":""},{"location":"concepts/architecture/#pipeline","title":"Pipeline","text":"<p>Every log message flows through a three-stage pipeline:</p> <pre><code> Logger                          Handler(s)                   Formatter\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510          \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510          \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502 log:info()  \u2502  msg,    \u2502  handler:handle  \u2502  msg,    \u2502  formatter:fmt   \u2502\n\u2502 log:warn()  \u2502 \u2500\u2500level\u2500\u2500\u25b6 (msg, extra,     \u2502 \u2500\u2500extra\u2500\u2500\u25b6 + {token}        \u2502\n\u2502 log:error() \u2502  extra    \u2502   level)         \u2502          \u2502  substitution    \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518          \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518          \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                                 \u2502                              \u2502\n                                 \u2502                     formatted string\n                                 \u2502                              \u2502\n                                 \u25bc                              \u25bc\n                          Output device                Final output\n                          (term, file,                  (written,\n                           modem, ws, etc.)             transmitted, etc.)\n</code></pre> <p>A Logger holds a minimum level and a list of Handler objects. Each Handler owns its own Formatter. When you call a logging method (e.g. <code>log:info(...)</code>):</p> <ol> <li><code>Logger:log()</code> builds an extra table with default keys (<code>message</code>, <code>level</code>,    <code>loggername</code>).</li> <li>If the message's level weight is below the logger's threshold, it is dropped    immediately.</li> <li>Otherwise, every handler's <code>:handle(msg, extra, level)</code> is called in sequence.</li> <li>Each handler's <code>:format(msg, extra)</code> method substitutes <code>{token}</code> patterns using the    extra table and returns a string. The handler then writes or transmits that string.</li> </ol> <pre><code>local logger = require(\"logger.lua\")\n\nlocal log = logger.new(\"multi\")\n\n--  colored terminal\nlocal termH = logger.ColoredTerminalHandler()\nlog:addHandler(termH)\n\n-- rotating file\nlocal fileH = logger.RotatingFileHandler(\n    logger.Formatter(),           -- default format\n    \"log.txt\",                    -- filename\n    4096,                         -- 4 KB max\n    3                             -- 3 backups\n)\nlog:addHandler(fileH)\n\nlog:info(\"Both handlers fire for this message\")\n</code></pre> <p>Default handler</p> <p><code>logger.new()</code> attaches a default <code>TerminalHandler(Formatter)</code> automatically. Pass <code>true</code> as the second argument to remove it: <code>logger.new(\"mine\", true)</code>.</p>"},{"location":"concepts/architecture/#key-types","title":"Key Types","text":"Component Role Created By Logger Filters by level, dispatches to handlers <code>logger.new(name)</code> Handler Receives <code>(msg, extra, level)</code>, formats &amp; outputs <code>logger.Handler(...)</code> Formatter Holds template string &amp; date format, does <code>{token}</code> substitution <code>logger.Formatter(fmt, datefmt)</code> Level A <code>{weight, name}</code> table, e.g. <code>{2, \"INFO\"}</code> <code>logger.INFO</code>, etc."},{"location":"concepts/architecture/#related","title":"Related","text":"<ul> <li>Log Levels  how level filtering works</li> <li>The Formatter  template syntax and date formatting</li> <li>Handlers Overview  all built-in handlers</li> </ul>"},{"location":"concepts/formatter/","title":"The Formatter","text":"<p>A Formatter holds a template string and a date format string. Every Handler calls <code>formatter:format(msg, extra)</code> to produce the final output string.</p>"},{"location":"concepts/formatter/#default-template","title":"Default Template","text":"<pre><code>[{loggername}] {asctime} [{level}] {message}\n</code></pre> <p>With the default <code>datefmt</code> of <code>%Y/%m/%e %H:%M:%S</code>, a typical line looks like:</p> <pre><code>[myApp] 2026/07/06 14:30:01 [INFO] System ready\n</code></pre>"},{"location":"concepts/formatter/#token-substitution","title":"<code>{token}</code> Substitution","text":"<p>The <code>extra</code> table is the source for all <code>{token}</code> replacement. The Logger populates three keys before passing to handlers:</p> Extra Key Populated By Value <code>message</code> <code>Logger:log()</code> The raw message string <code>level</code> <code>Logger:log()</code> The level name, e.g. <code>\"INFO\"</code> <code>loggername</code> <code>Logger:log()</code> The logger's name (set at construction) <code>asctime</code> The formatter Auto-set in <code>:format()</code> if absent <p>The Logger sets <code>extra.message = msg</code> before dispatching to handlers. When the formatter's <code>{message}</code> token is substituted, it expands to the raw message  which may itself contain <code>{tokens}</code> for other extra keys to fill.</p> <p>Any additional key you add to <code>extra</code> becomes available as a <code>{token}</code>:</p> <pre><code>local logger = require(\"logger.lua\")\n\nlocal log = logger.new(\"miner\", true)  -- no default handler\n\n-- Custom formatter with a new token\nlocal fmt = logger.Formatter(\n    \"[{loggername}] [{level}] ({computerID}) {message}\"\n)\n\nlocal termH = logger.TerminalHandler(fmt)\nlog:addHandler(termH)\n\nlog:info(\"Tunnel complete\", {\n    computerID = os.getComputerID()\n})\n-- Output: [miner] [INFO] (123) Tunnel complete\n</code></pre> <p>Any key works</p> <p>There's no limit on token names  the formatter does a simple <code>formatted:gsub(\"{\"..key..\"}\", tostring(value))</code> for every key in <code>extra</code>.</p> <p><code>{message}</code> is substituted first, before the <code>pairs()</code> loop, so it is always resolved regardless of iteration order.</p>"},{"location":"concepts/formatter/#tokens-in-message-strings","title":"<code>{tokens}</code> in Message Strings","text":"<p>Because the Logger sets <code>extra.message = msg</code> before dispatching, the message string can itself contain <code>{tokens}</code> that reference other extra keys:</p> <pre><code>local logger = dofile(\"logger.lua\")\nlocal log = logger.new(\"miner\")\n\nlog:warn(\"Fuel critically low: {fuel}\", { fuel = 5 })\n-- Output: [miner] 2026/07/06 14:30:01 [WARNING] Fuel critically low: 5\n</code></pre> <p>At runtime: <code>extra.message</code> is set to <code>\"Fuel critically low: {fuel}\"</code> by the Logger. The formatter substitutes <code>{message}</code> first (before the <code>pairs()</code> loop), expanding it to the raw string. Then the <code>pairs()</code> loop finds <code>{fuel}</code> in the expanded message and replaces it.</p> <p>You can use this to keep your code concise:</p> <pre><code>-- Instead of string concatenation:\nlog:info(\"Harvest cycle \" .. cycle .. \" complete\")\n\n-- Use a token in the message:\nlog:info(\"Harvest cycle {cycle} complete\", { cycle = cycle })\n</code></pre> <p>Reliable</p> <p>Because <code>{message}</code> is substituted explicitly before the <code>pairs()</code> loop, tokens in the message string are always resolved  no iteration order dependency.</p>"},{"location":"concepts/formatter/#loggerformatterfmt-datefmt-loggerformatter","title":"<code>logger.Formatter(fmt, datefmt)</code> {: #loggerformatter}","text":"<p>Constructs a new Formatter. Both parameters are optional.</p> Parameter Type Default <code>fmt</code> <code>string</code> <code>\"[{loggername}] {asctime} [{level}] {message}\"</code> <code>datefmt</code> <code>string</code> <code>\"%Y/%m/%e %H:%M:%S\"</code> <p>Omitting a parameter keeps the default</p> <pre><code>local f1 = logger.Formatter()                         -- both defaults\nlocal f2 = logger.Formatter(\"{level}: {message}\")     -- custom fmt, default datefmt\nlocal f3 = logger.Formatter(nil, \"%H:%M:%S\")          -- default fmt, custom datefmt\n</code></pre>"},{"location":"concepts/formatter/#colors-table","title":"<code>colors</code> Table","text":"<p>Every Formatter (including the default prototype) has a <code>colors</code> table mapping level names to <code>{fg, bg}</code> color pairs. Used by ColoredTerminalHandler.</p> Level Foreground <code>DEBUG</code> <code>colors.blue</code> <code>INFO</code> <code>colors.white</code> <code>WARNING</code> <code>colors.yellow</code> <code>ERROR</code> <code>colors.red</code> <code>CRITICAL</code> <code>colors.purple</code> <p>To customize colors for a single handler, build a new Formatter and override the <code>colors</code> table:</p> <pre><code>local fmt = logger.Formatter()\nfmt.colors = {\n    INFO = { fg = colors.green },\n    ERROR = { fg = colors.red, bg = colors.black }\n}\n</code></pre>"},{"location":"concepts/formatter/#related","title":"Related","text":"<ul> <li>Architecture  where formatting fits in the pipeline</li> <li>ColoredTerminalHandler  the handler that uses <code>colors</code></li> <li>Levels  level constants</li> </ul>"},{"location":"concepts/levels/","title":"Log Levels","text":"<p>Every message carries a level  a small table with a numeric weight and a string name. Levels control which messages are processed and which are discarded.</p>"},{"location":"concepts/levels/#built-in-levels","title":"Built-in Levels","text":"Constant Weight Level Table <code>logger.DEBUG</code> 1 <code>{1, \"DEBUG\"}</code> <code>logger.INFO</code> 2 <code>{2, \"INFO\"}</code> <code>logger.WARNING</code> 3 <code>{3, \"WARNING\"}</code> <code>logger.ERROR</code> 4 <code>{4, \"ERROR\"}</code> <code>logger.CRITICAL</code> 5 <code>{5, \"CRITICAL\"}</code> <p>A level is considered valid by the library when it is a table with a number at index 1 and a string at index 2.</p>"},{"location":"concepts/levels/#setlevellevel-setlevel","title":"<code>setLevel(level)</code> {: #setlevel}","text":"<p>Sets the logger's minimum threshold. Only messages at or above this weight will be processed.</p> <pre><code>local logger = require(\"logger.lua\")\nlocal log = logger.new(\"quiet\")\n\nlog:setLevel(logger.WARNING)   -- only WARNING and above\n\nlog:debug(\"you won't see this\")\nlog:info(\"or this\")\nlog:warn(\"but you WILL see this\")   -- printed\nlog:error(\"and this\")               -- printed\n</code></pre> <p>Returns <code>true</code> if the level was valid, <code>false</code> otherwise.</p>"},{"location":"concepts/levels/#isenabledforlevel-isenabledfor","title":"<code>isEnabledFor(level)</code> {: #isenabledfor}","text":"<p>Check whether a message at the given level would be logged:</p> <pre><code>if log:isEnabledFor(logger.DEBUG) then\n    log:debug(\"info is enabled\")\nend\n</code></pre> <p>Returns <code>true</code> when <code>level[1] &gt;= current_level[1]</code> and the level is valid.</p>"},{"location":"concepts/levels/#geteffectivelevel-geteffectivelevel","title":"<code>getEffectiveLevel()</code> {: #geteffectivelevel}","text":"<p>Returns the level table currently in effect:</p> <pre><code>local lv = log:getEffectiveLevel()\nprint(lv[1]) -- example 1\nprint(lv[2])  -- example \"INFO\"\n</code></pre>"},{"location":"concepts/levels/#how-filtering-works","title":"How Filtering Works","text":"<p>Inside <code>Logger:log()</code>:</p> <ol> <li>If the level table is invalid, the message is dropped.</li> <li>If <code>level[1] &lt; self.level[1]</code>, the message is dropped.</li> <li>Otherwise, every handler's <code>:handle()</code> is called.</li> </ol> <p>This means a handler never sees messages below the logger's threshold.</p> <pre><code>local logger = require(\"logger.lua\")\nlocal log = logger.new(\"demo\")\n\nlog:setLevel(logger.WARNING)\n\n-- These produce NO output:\nlog:debug(\"debug\")\nlog:info(\"info\")\n\n-- These DO produce output:\nlog:warn(\"warn\")\nlog:error(\"error\")\nlog:critical(\"critical\")\n</code></pre>"},{"location":"concepts/levels/#creating-custom-levels","title":"Creating Custom Levels","text":"<p>Any <code>{number, string}</code> table works. You could define:</p> <pre><code>local TRACE = {0, \"TRACE\"}\nlocal FATAL = {6, \"FATAL\"}\n\nlog:setLevel(TRACE)\nlog:log(\"trace message\", TRACE)\n</code></pre> <p>Levels are just data</p> <p>There is no registration step  any valid level table is accepted. The <code>colors</code> table in the default Formatter only has entries for the five built-in levels, so custom levels won't get colored output unless you add them.</p>"},{"location":"concepts/levels/#related","title":"Related","text":"<ul> <li>The Formatter  how <code>{level}</code> is substituted</li> <li>API Reference <code>setLevel</code>, <code>isEnabledFor</code>, <code>getEffectiveLevel</code></li> </ul>"},{"location":"cookbook/multi-handler/","title":"Multi-Handler Setups","text":"<p>The Logger dispatches every message to all registered handlers. This makes it trivial to send logs to multiple destinations simultaneously.</p>"},{"location":"cookbook/multi-handler/#colored-terminal-rotating-file-websocket","title":"Colored Terminal + Rotating File + WebSocket","text":"<pre><code>local logger = require(\"logger.lua\")\n\n-- Logger without default handler\nlocal log = logger.new(\"multiApp\", true)\nlog:setLevel(logger.DEBUG)\n\n-- 1. Colored console output\nlocal console = logger.ColoredTerminalHandler()\nlog:addHandler(console)\n\n-- 2. Rotating file \nlocal fileFmt = logger.Formatter(\"{asctime} [{level}] {message}\")\nlocal fileH = logger.RotatingFileHandler(fileFmt, \"app.log\", 8192, 3)\nlog:addHandler(fileH)\n\n-- 3. WebSocket to remote log server\nlocal ws, err = http.websocket(\"ws://logs.example.com/ingest\")\nif ws then\n    local wsFmt = logger.Formatter(\"[{loggername}] {message}\")\n    local wsH = logger.WebsocketHandler(wsFmt, ws)\n    log:addHandler(wsH)\nend\n\n-- All three handlers fire for every message\nlog:debug(\"Verbose diagnostic\")\nlog:info(\"User logged in\")\nlog:warn(\"Disk at 85%\")\nlog:error(\"Connection timeout\")\n</code></pre> <p>Each handler can have its own Formatter with different templates and date formats:</p> <pre><code>-- Terminal: short format\nlocal termFmt = logger.Formatter(\"{level}: {message}\")\n\n-- File: full detail with timestamp\nlocal fileFmt = logger.Formatter(\"{asctime} [{level}] ({loggername}) {message}\")\n\n-- WebSocket: JSON-like key-value\nlocal wsFmt = logger.Formatter(\"{loggername}|{level}|{message}\")\n</code></pre>"},{"location":"cookbook/multi-handler/#separating-levels-by-handler","title":"Separating Levels by Handler","text":"<p>Because handlers don't filter by level (only the Logger does), you can create multiple loggers at different levels targeting different outputs:</p> <pre><code>local logger = require(\"logger.lua\")\n\n-- Errors-only logger for alerts\nlocal alertLog = logger.new(\"alert\", true)\nalertLog:setLevel(logger.ERROR)\nlocal alertFile = logger.FileHandler(nil, \"alerts.log\")\nalertLog:addHandler(alertFile)\n\n-- Full debug logger for development\nlocal devLog = logger.new(\"dev\", true)\ndevLog:setLevel(logger.DEBUG)\nlocal devConsole = logger.ColoredTerminalHandler()\ndevLog:addHandler(devConsole)\n\n-- Use both in the same program\nalertLog:info(\"You won't see this in alerts.log\")  -- filtered by alertLog's level\ndevLog:info(\"You WILL see this on console\")\nalertLog:error(\"But you WILL see this in alerts.log\")\n</code></pre>"},{"location":"cookbook/multi-handler/#related","title":"Related","text":"<ul> <li>Architecture  how dispatch works</li> <li>Log Levels  level filtering</li> </ul>"},{"location":"cookbook/rotating-turtle-logs/","title":"Rotating Logs on Turtles","text":"<p>Computers have limited disk space. Using <code>RotatingFileHandler</code> prevents a single log file from filling the computer's drive.</p>"},{"location":"cookbook/rotating-turtle-logs/#mining-turtle-with-size-capped-log","title":"Mining Turtle with Size-Capped Log","text":"<pre><code>-- miner.lua\nlocal logger = require(\"logger.lua\")\n\nlocal fmt = logger.Formatter(\"{asctime} [{level}] {message}\")\nlocal h = logger.RotatingFileHandler(fmt, \"miner.log\", 4096, 2)\nlocal log = logger.new(\"miner\", true)\nlog:addHandler(h)\n\nlog:info(\"Mining program started\")\n\nlocal blocksMined = 0\nlocal fuel = 0\n\nfor i = 1, 100 do\n    -- fake mining\n    blocksMined = blocksMined + 1\n    fuel = fuel - 1\n\n    if blocksMined % 10 == 0 then\n        log:info(\"Progress: mined \" .. blocksMined .. \" blocks\", {\n            blocks = blocksMined,\n            fuel = fuel\n        })\n    end\n\n    if fuel &lt; 50 then\n        log:warn(\"Fuel critically low: {fuel}\",{fuel=fuel})\n    end\n\n    if fuel &lt;= 0 then\n        log:critical(\"Out of fuel!!\")\n        break\n    end\nend\n\nlog:info(\"Mining session ended. Total blocks: \" .. blocksMined)\n</code></pre> <p>After running, the turtle's disk will contain at most <code>~4 KB + 2 backups</code> of log data:</p> <pre><code>miner.log       \u2190 current session (being written)\nminer.log.1     \u2190 previous session\nminer.log.2     \u2190 session before that\n</code></pre>"},{"location":"cookbook/rotating-turtle-logs/#farming-turtle","title":"Farming Turtle","text":"<pre><code>-- farmer.lua\nlocal logger = require(\"logger.lua\")\n\nlocal fmt = logger.Formatter(\"{asctime} [{level}] {message}\")\nlocal h = logger.RotatingFileHandler(fmt, \"farm.log\", 2048, 1)\nlocal log = logger.new(\"farmer\", true)\nlog:addHandler(h)\n\nlog:info(\"Farm program started\")\n\nlocal harvests = 0\nfor cycle = 1, 50 do\n    harvests = harvests + 8  -- 8 crops per cycle\n    log:info(\"Harvest cycle {cycle} complete\", {\n        totalHarvested = harvests,\n        cycle=cycle\n    })\nend\n\nlog:info(\"Farm run complete: {harvests} items harvested\",{harvests=harvests})\n</code></pre> <p>With only 2 KB cap and 1 backup, this uses minimal disk space.</p>"},{"location":"cookbook/rotating-turtle-logs/#combining-console-file","title":"Combining Console + File","text":"<pre><code>local logger = require(\"logger.lua\")\n\nlocal log = logger.new(\"turtle\")\n\n\n-- rotating file\nlocal fileFmt = logger.Formatter(\"{asctime} [{level}] {message}\")\nlocal fileH = logger.RotatingFileHandler(fileFmt, \"turtle.log\", 4096, 3)\nlog:addHandler(fileH)\n\nlog:info(\"Now logging to both terminal and rotating file\")\n</code></pre> <p>Since <code>logger.new()</code> attaches a default <code>TerminalHandler</code>, you only need to add the file handler.</p>"},{"location":"cookbook/rotating-turtle-logs/#related","title":"Related","text":"<ul> <li>RotatingFileHandler  handler details</li> <li>Multi-Handler Setups  combining handler types</li> </ul>"},{"location":"handlers/","title":"Handlers Overview","text":"<p>A Handler receives <code>(msg, extra, level)</code> from a Logger, formats the message using its own Formatter, and writes/transmits it somewhere.</p>"},{"location":"handlers/#common-interface","title":"Common Interface","text":"<p>Every handler  built-in or custom  must implement three methods to work with <code>Logger:addHandler()</code>:</p> Method Purpose <code>:handle(msg, extra, level)</code> Produce output (write to file, send over network, etc.) <code>:format(msg, extra)</code> Substitute <code>{token}</code> patterns using the formatter <code>:addTo(logger)</code> Insert self into <code>logger.handlers</code> (typically <code>table.insert(logger.handlers, self)</code>) <p><code>addTo</code> vs <code>addHandler</code></p> <p>You can add a handler to a logger with either <code>handler:addTo(logger)</code> or <code>logger:addHandler(handler)</code>. Both do <code>table.insert(logger.handlers, self)</code>.</p>"},{"location":"handlers/#all-built-in-handlers","title":"All Built-in Handlers","text":"Handler Output Page <code>TerminalHandler</code> Plain text to terminal or monitor TerminalHandler <code>ColoredTerminalHandler</code> Per-level colored text with word-wrapping ColoredTerminalHandler <code>FileHandler</code> Append to a file FileHandler <code>RotatingFileHandler</code> File rotation by byte-size RotatingFileHandler <code>TimedRotatingFileHandler</code> File rotation by time interval TimedRotatingFileHandler <code>WebsocketHandler</code> Formatted string over websocket WebsocketHandler <code>RawWebsocketHandler</code> Raw JSON <code>{msg, extra}</code> over websocket (no formatter) WebsocketHandler <code>ModemHandler</code> Formatted string over rednet/modem ModemHandler <code>RawModemHandler</code> Raw <code>{msg, extra}</code> table over modem (no formatter) ModemHandler"},{"location":"handlers/#related","title":"Related","text":"<ul> <li>Architecture  how handlers fit in the pipeline</li> <li>The Formatter  how <code>{token}</code> substitution works</li> <li>Writing a Custom Handler  build your own</li> </ul>"},{"location":"handlers/colored-terminal/","title":"ColoredTerminalHandler","text":"<p>Like TerminalHandler, but applies per-level foreground/background colors and uses a word-wrapping writer.</p> <pre><code>ColoredTerminalHandler(formatter, terminal)\n</code></pre>"},{"location":"handlers/colored-terminal/#parameters","title":"Parameters","text":"Parameter Type Default Description <code>formatter</code> <code>table</code>|<code>nil</code> Copied from default prototype Formatter whose <code>colors</code> table drives per-level styling <code>terminal</code> <code>table</code>|<code>nil</code> <code>term.current()</code> Terminal or monitor object"},{"location":"handlers/colored-terminal/#how-colors-work","title":"How Colors Work","text":"<p>When <code>:handle()</code> is called, it reads the level name, looks up <code>formatter.colors[LEVEL]</code>, and applies the <code>fg</code> / <code>bg</code> values before writing. The previous colors are restored after each line.</p> <p>The default <code>colors</code> table:</p> Level Foreground Background <code>DEBUG</code> <code>colors.blue</code> (none) <code>INFO</code> <code>colors.white</code> (none) <code>WARNING</code> <code>colors.yellow</code> (none) <code>ERROR</code> <code>colors.red</code> (none) <code>CRITICAL</code> <code>colors.purple</code> (none)"},{"location":"handlers/colored-terminal/#example-default-colors","title":"Example: Default Colors","text":"<pre><code>local logger = require(\"logger.lua\")\n\nlocal h = logger.ColoredTerminalHandler()\nlocal log = logger.new(\"miner\", true)\nlog:addHandler(h)\n\nlog:debug(\"Starting scan\")\nlog:info(\"Found deposit\")\nlog:warn(\"Low fuel\")\nlog:error(\"Chest full\")\nlog:critical(\"Stuck in loop\")\n</code></pre> <p>Each level prints in its configured color automatically.</p>"},{"location":"handlers/colored-terminal/#example-custom-color-scheme","title":"Example: Custom Color Scheme","text":"<pre><code>local logger = require(\"logger.lua\")\n\nlocal fmt = logger.Formatter()\nfmt.colors = {\n    DEBUG    = { fg = colors.gray },\n    INFO     = { fg = colors.green },\n    WARNING  = { fg = colors.orange },\n    ERROR    = { fg = colors.red,   bg = colors.black },\n    CRITICAL = { fg = colors.white, bg = colors.red },\n}\n\nlocal h = logger.ColoredTerminalHandler(fmt)\nlocal log = logger.new(\"server\", true)\nlog:addHandler(h)\n\nlog:info(\"Green means good\")\nlog:critical(\"Red alert!\")\n</code></pre>"},{"location":"handlers/colored-terminal/#word-wrapping","title":"Word-Wrapping","text":"<p>The internal <code>writeLine()</code> function performs word-wrapping: long lines are broken at word boundaries to fit the terminal width, and the terminal scrolls when the cursor reaches the bottom. Multi-line messages (<code>\\n</code>) are handled line-by-line.</p>"},{"location":"handlers/colored-terminal/#notes","title":"Notes","text":"<ul> <li>The <code>colors</code> table is read from the Formatter, not from the handler itself. To   customize per-handler, create a new Formatter and set its <code>colors</code>.</li> <li>Background colors are optional  if <code>color.bg</code> is nil, the background is not changed.</li> <li>See Formatter <code>colors</code> table for the default structure.</li> </ul>"},{"location":"handlers/colored-terminal/#related","title":"Related","text":"<ul> <li>TerminalHandler  plain-text terminal output</li> <li>The Formatter  the <code>colors</code> table, token substitution</li> </ul>"},{"location":"handlers/custom/","title":"Writing a Custom Handler","text":"<p>A handler just needs three methods to work with <code>Logger:addHandler()</code>:</p> Method Signature Purpose <code>:handle</code> <code>(msg, extra, level)</code> Produce output <code>:format</code> <code>(msg, extra)</code> \u2192 <code>string</code> Apply template substitution (optional for raw handlers) <code>:addTo</code> <code>(logger)</code> Register with a logger"},{"location":"handlers/custom/#minimal-shape","title":"Minimal Shape","text":"<pre><code>local MyHandler = {}\n\nfunction MyHandler.new(formatter)\n    local self = {}\n\n    self.formatter = formatter or logger.Formatter()\n\n    function self:format(msg, extra)\n        if not extra.asctime then\n            extra.asctime = os.date(self.formatter.datefmt)\n        end\n        local formatted = self.formatter.fmt\n        formatted = formatted:gsub(\"{message}\", tostring(message))\n        for k, v in pairs(extra) do\n            formatted = formatted:gsub(\"{\" .. k .. \"}\", tostring(v))\n        end\n        return formatted\n    end\n\n    function self:handle(msg, extra, level)\n        -- Your output logic here\n    end\n\n    function self:addTo(logger)\n        table.insert(logger.handlers, self)\n    end\n\n    return self\nend\n</code></pre>"},{"location":"handlers/custom/#worked-example-discord-webhook-via-httppost","title":"Worked Example: Discord Webhook via <code>http.post</code>","text":"<p>This handler sends each log line as a Discord embed using an <code>http.post</code> request:</p> <pre><code>local logger = dofile(\"logger.lua\")\n\nlocal DiscordHandler = {}\n\nfunction DiscordHandler.new(webhookUrl, formatter)\n    local self = {}\n\n    self.url = webhookUrl\n    self.formatter = formatter or logger.Formatter()\n\n    -- Color mapping for Discord embed side-bar\n    local embedColors = {\n        DEBUG    = 0x555555,\n        INFO     = 0x00AA00,\n        WARNING  = 0xFFAA00,\n        ERROR    = 0xAA0000,\n        CRITICAL = 0xAA00AA,\n    }\n\n    function self:format(msg, extra)\n        if not extra.asctime then\n            extra.asctime = os.date(self.formatter.datefmt)\n        end\n        local formatted = self.formatter.fmt\n        for k, v in pairs(extra) do\n            formatted = formatted:gsub(\"{\" .. k .. \"}\", tostring(v))\n        end\n        return formatted\n    end\n\n    function self:handle(msg, extra, level)\n        local text = self:format(msg, extra)\n        local levelName = level and level[2] or \"UNKNOWN\"\n        local color = embedColors[levelName] or 0x000000\n\n        local payload = textutils.serializeJSON({\n            embeds = {{\n                description = text,\n                color = color,\n                footer = { text = levelName },\n                timestamp = os.date(\"!%Y-%m-%dT%H:%M:%SZ\")\n            }}\n        })\n\n        http.post(self.url, payload, {\n            [\"Content-Type\"] = \"application/json\"\n        })\n    end\n\n    function self:addTo(logger)\n        table.insert(logger.handlers, self)\n    end\n\n    return self\nend\n\n-- Usage\nlocal webhook = \"https://discord.com/api/webhooks/your-id/your-token\"\nlocal h = DiscordHandler.new(webhook)\nlocal log = logger.new(\"server\")\nlog:addHandler(h)\n\nlog:critical(\"Server room on fire\") \n</code></pre>"},{"location":"handlers/custom/#raw-handlers","title":"Raw Handlers","text":"<p>A handler that doesn't use a formatter can skip the <code>:format</code> method entirely:</p> <pre><code>function MyRawHandler:handle(msg, extra, level)\n    -- send raw data\n    modem.transmit(channel, channel, {msg, extra})\nend\n</code></pre> <p>Both <code>RawWebsocketHandler</code> and <code>RawModemHandler</code> follow this pattern  they accept a <code>formatter</code> parameter for API consistency but never call <code>:format()</code>.</p>"},{"location":"handlers/custom/#related","title":"Related","text":"<ul> <li>Handlers Overview  common interface and all built-in handlers</li> <li>The Formatter <code>{token}</code> substitution details</li> </ul>"},{"location":"handlers/file/","title":"FileHandler","text":"<p>Writes formatted log lines to a file on the computer's filesystem.</p> <pre><code>FileHandler(formatter, filename, mode, delay)\n</code></pre>"},{"location":"handlers/file/#parameters","title":"Parameters","text":"Parameter Type Default Description <code>formatter</code> <code>table</code>|<code>nil</code> Copied from default Formatter for <code>{token}</code> substitution <code>filename</code> <code>string</code> (required) Path to the log file <code>mode</code> <code>string</code>|<code>nil</code> <code>\"a\"</code> File open mode (<code>\"a\"</code> for append, <code>\"w\"</code> for write) <code>delay</code> <code>boolean</code>|<code>nil</code> <code>false</code> If <code>true</code>, file is not opened until first write"},{"location":"handlers/file/#example-crash-log","title":"Example: Crash Log","text":"<pre><code>local logger = dofile(\"logger.lua\")\n\nlocal fmt = logger.Formatter(\"{asctime} [{level}] {message}\")\nlocal h = logger.FileHandler(fmt, \"crash.log\", \"a\", true)\nlocal log = logger.new(\"app\", true)\n\nlog:addHandler(h)\n\n-- Simulate a crash sequence\nlog:error(\"Unhandled exception in render loop\")\nlog:error(\"Stack traceback: [redacted]\")\n</code></pre> <p>The file <code>crash.log</code> will contain:</p> <pre><code>2026/07/06 14:30:01 [ERROR] Unhandled exception in render loop\n2026/07/06 14:30:01 [ERROR] Stack traceback: [redacted]\n</code></pre>"},{"location":"handlers/file/#using-the-same-file-across-restarts","title":"Using the Same File Across Restarts","text":"<p>With <code>mode = \"a\"</code> (the default), each run appends to the file:</p> <pre><code>local h = logger.FileHandler(nil, \"server.log\")\n</code></pre> <p>With <code>mode = \"w\"</code>, the file is truncated each time the handler is constructed (or on first write if <code>delay</code> is true):</p> <pre><code>local h = logger.FileHandler(nil, \"session.log\", \"w\")\n</code></pre>"},{"location":"handlers/file/#notes","title":"Notes","text":"<ul> <li>If <code>delay</code> is <code>true</code>, the file handle is opened lazily on the first <code>:handle()</code> call.   Useful when you construct the handler early but don't want a file descriptor sitting   open.</li> <li>The <code>mode</code> parameter is not validated by the handler  see Known Quirks   for details.</li> <li>Each call to <code>:handle()</code> opens the file (if not already open), writes, and keeps the   file handle open. Close is only explicit (e.g. via a <code>RotatingFileHandler</code> rotation).</li> </ul>"},{"location":"handlers/file/#related","title":"Related","text":"<ul> <li>RotatingFileHandler  size-based log rotation</li> <li>TimedRotatingFileHandler  time-based log rotation</li> <li>Formatter  format tokens</li> </ul>"},{"location":"handlers/modem/","title":"ModemHandler / RawModemHandler","text":"<p>Transmit log messages over a ComputerCraft modem (wired or wireless). Useful for aggregating logs from multiple computers onto a central logging server.</p>"},{"location":"handlers/modem/#modemhandler","title":"ModemHandler","text":"<p>Transmits the formatted string on a specified channel.</p> <pre><code>ModemHandler(formatter, modem, channel)\n</code></pre> Parameter Type Default Description <code>formatter</code> <code>table</code>|<code>nil</code> Copied from default Formatter for <code>{token}</code> substitution <code>modem</code> <code>table</code> (required) A wrapped modem peripheral <code>channel</code> <code>number</code> (required) Channel to transmit on"},{"location":"handlers/modem/#rawmodemhandler","title":"RawModemHandler","text":"<p>Transmits the raw Lua table <code>{msg, extra}</code>  no formatter applied. The receiving end can inspect the full structured data.</p> <pre><code>RawModemHandler(formatter, modem, channel)\n</code></pre> Parameter Type Description <code>formatter</code> <code>any</code> Ignored (accepted but never used) <code>modem</code> <code>table</code> A wrapped modem peripheral <code>channel</code> <code>number</code> Channel to transmit on"},{"location":"handlers/modem/#example-wireless-logger","title":"Example: Wireless Logger","text":"<p>Transmitter (mining turtle):</p> <pre><code>local logger = require(\"logger.lua\")\n\nlocal modem = peripheral.wrap(\"top\")\nlocal fmt = logger.Formatter(\"[{loggername}] {message}\")\n\nlocal h = logger.ModemHandler(fmt, modem, 42)\nlocal log = logger.new(\"turtle1\", true)\nlog:addHandler(h)\n\nlog:info(\"Starting mining operation\")\n</code></pre> <p>Receiver (central computer):</p> <pre><code>local modem = peripheral.wrap(\"back\")\nmodem.open(42)\n\nwhile true do\n    local event, side, channel, replyChannel, message, distance = os.pullEvent(\"modem_message\")\n    if channel == 42 then\n        print(\"Received: \" .. message)\n    end\nend\n</code></pre>"},{"location":"handlers/modem/#example-raw-modem-with-structured-data","title":"Example: Raw Modem with Structured Data","text":"<pre><code>local logger = require(\"logger.lua\")\n\nlocal modem = peripheral.wrap(\"right\")\nlocal h = logger.RawModemHandler(nil, modem, 99)\nlocal log = logger.new(\"sensor\", true)\nlog:addHandler(h)\n\nlog:info(\"Temperature spike\", { temp = 87.3, tank = \"reactor_1\" })\n-- Transmits: {\"Temperature spike\", {message=\"Temperature spike\", level=\"INFO\",\n--              loggername=\"sensor\", temp=87.3, tank=\"reactor_1\"}}\n</code></pre> <p>Receiving side:</p> <pre><code>local modem = peripheral.wrap(\"left\")\nmodem.open(99)\n\nwhile true do\n    local _, _, channel, _, data = os.pullEvent(\"modem_message\")\n    if channel == 99 then\n        local msg = data[1]\n        local extra = data[2]\n        print((\"%s [%s] %s\"):format(extra.loggername, extra.level, msg))\n        if extra.temp then\n            print(\"  Temperature: \" .. extra.temp)\n        end\n    end\nend\n</code></pre>"},{"location":"handlers/modem/#notes","title":"Notes","text":"<ul> <li>The <code>modem</code> must already be a wrapped peripheral object  the handler does not call   <code>peripheral.wrap()</code> for you.</li> <li>No validation is performed on the modem object; a bad reference errors inside   <code>:handle()</code>.</li> </ul>"},{"location":"handlers/modem/#related","title":"Related","text":"<ul> <li>WebsocketHandler / RawWebsocketHandler  network logging over websockets</li> </ul>"},{"location":"handlers/rotating-file/","title":"RotatingFileHandler","text":"<p>Like FileHandler, but automatically rotates the log file when it exceeds a size threshold, keeping a configurable number of backup files.</p> <pre><code>RotatingFileHandler(formatter, filename, maxByte, backupCount, mode, delay)\n</code></pre>"},{"location":"handlers/rotating-file/#parameters","title":"Parameters","text":"Parameter Type Default Description <code>formatter</code> <code>table</code>|<code>nil</code> Copied from default Formatter for <code>{token}</code> substitution <code>filename</code> <code>string</code> (required) Path to the log file <code>maxByte</code> <code>number</code> (required) Maximum size in bytes before rotation triggers <code>backupCount</code> <code>number</code> (required) Number of old log files to retain (must be &gt; 0 for rotation) <code>mode</code> <code>string</code>|<code>nil</code> <code>\"a\"</code> File open mode <code>delay</code> <code>boolean</code>|<code>nil</code> <code>false</code> If <code>true</code>, defers file open until first write"},{"location":"handlers/rotating-file/#rotation-scheme","title":"Rotation Scheme","text":"<p>When <code>self.length + #newLine &gt;= maxByte</code>:</p> <ol> <li>Close current file.</li> <li>Delete <code>filename.N</code> (the oldest backup).</li> <li>Shift <code>filename.(N-1)</code> \u2192 <code>filename.N</code>, ..., <code>filename.1</code> \u2192 <code>filename.2</code>.</li> <li>Rename current <code>filename</code> \u2192 <code>filename.1</code>.</li> <li>Open fresh <code>filename</code> and reset length counter.</li> </ol> <p>So with <code>backupCount = 3</code>, after several rotations you might see:</p> <pre><code>log.txt       \u2190 currently being written\nlog.txt.1     \u2190 most recent full file\nlog.txt.2\nlog.txt.3     \u2190 oldest\n</code></pre>"},{"location":"handlers/rotating-file/#important-backupcount-must-be-0","title":"Important: <code>backupCount</code> Must Be &gt; 0","text":"<p>The rotation check is guarded by <code>if maxByte and backupCount and backupCount &gt; 0</code>. If <code>backupCount</code> is 0 or <code>nil</code>, rotation never happens and the handler behaves like a plain <code>FileHandler</code>.</p>"},{"location":"handlers/rotating-file/#example-8-kb-cap-with-3-backups","title":"Example: 8 KB Cap with 3 Backups","text":"<pre><code>local logger = require(\"logger.lua\")\n\nlocal fmt = logger.Formatter(\"{asctime} [{level}] {message}\")\nlocal h = logger.RotatingFileHandler(fmt, \"turtle.log\", 8192, 3)\nlocal log = logger.new(\"turtle\", true)\nlog:addHandler(h)\n\nfor i = 1, 500 do\n    log:info(\"Mined block #\" .. i)\nend\n-- File at most ~8 KB; up to 3 old logs preserved as turtle.log.1, .2, .3\n</code></pre>"},{"location":"handlers/rotating-file/#detecting-existing-file-size","title":"Detecting Existing File Size","text":"<p>On construction, if the file already exists, the handler reads its length via <code>fileReader:seek(\"end\")</code> so the size cap is enforced across restarts.</p>"},{"location":"handlers/rotating-file/#notes","title":"Notes","text":"<ul> <li><code>maxByte</code> is compared against a running <code>self.length</code> counter that tracks total   written bytes, not the filesystem-reported file size (except on initial load).</li> <li>The <code>.1</code> suffix is always the most recently rotated file.</li> </ul>"},{"location":"handlers/rotating-file/#related","title":"Related","text":"<ul> <li>FileHandler  simpler file logging without rotation</li> <li>TimedRotatingFileHandler  time-based rotation</li> <li>Rotating Logs on Turtles  real-world turtle example</li> </ul>"},{"location":"handlers/terminal/","title":"TerminalHandler","text":"<p>Sends formatted log lines to a ComputerCraft terminal (or any object that supports the terminal API  <code>getSize()</code>, <code>write()</code>, <code>getCursorPos()</code>, <code>setCursorPos()</code>, <code>scroll()</code>).</p> <pre><code>TerminalHandler(formatter, terminal)\n</code></pre>"},{"location":"handlers/terminal/#parameters","title":"Parameters","text":"Parameter Type Default Description <code>formatter</code> <code>table</code>|<code>nil</code> Copied from default prototype Formatter to use for <code>{token}</code> substitution <code>terminal</code> <code>table</code>|<code>nil</code> <code>term.current()</code> Terminal object to write to"},{"location":"handlers/terminal/#default-handler","title":"Default Handler","text":"<p><code>logger.new()</code> automatically attaches a <code>TerminalHandler(Formatter)</code> unless you pass <code>true</code> for the <code>RemoveDefaultHandle</code> parameter:</p> <pre><code>-- Default handler is present:\nlocal logA = logger.new(\"myApp\")       -- has default TerminalHandler\n\n-- No default handler:\nlocal logB = logger.new(\"myApp\", true) -- no handlers at all\n</code></pre>"},{"location":"handlers/terminal/#example-monitor-as-log-output","title":"Example: Monitor as Log Output","text":"<p>You can point a TerminalHandler at a monitor peripheral instead of the default terminal:</p> <pre><code>local logger = require(\"logger.lua\")\n\nlocal monitor = peripheral.wrap(\"top\")\nlocal fmt = logger.Formatter(\"{asctime} [{level}] {message}\")\n\nlocal h = logger.TerminalHandler(fmt, monitor)\nlocal log = logger.new(\"reactor\", true)\nlog:addHandler(h)\n\nlog:info(\"Reactor online\")\nlog:warn(\"Temperature approaching limit\")\n</code></pre>"},{"location":"handlers/terminal/#example-custom-formatter-with-terminalhandler","title":"Example: Custom Formatter with TerminalHandler","text":"<pre><code>local logger = require(\"logger.lua\")\n\nlocal fmt = logger.Formatter(\"[{loggername}] {message}\")\nlocal h = logger.TerminalHandler(fmt)\nlocal log = logger.new(\"minebot\", true)\nlog:addHandler(h)\n\nlog:info(\"Started mining cycle\")\n</code></pre>"},{"location":"handlers/terminal/#notes","title":"Notes","text":"<ul> <li>The handler uses <code>print(self:format(log, extra) .. \"\\n\")</code> to output. This means each   log line gets an extra blank line after it.</li> <li>No color, no word-wrapping  see ColoredTerminalHandler for   those features.</li> <li>If you pass a monitor, make sure it is already <code>peripheral.wrap()</code>'d before   constructing the handler.</li> </ul>"},{"location":"handlers/terminal/#related","title":"Related","text":"<ul> <li>ColoredTerminalHandler  colored, word-wrapped output</li> <li>Formatter <code>{token}</code> substitution</li> </ul>"},{"location":"handlers/timed-rotating-file/","title":"TimedRotatingFileHandler","text":"<p>Like FileHandler, but rotates the log file at scheduled time intervals (seconds, minutes, hours, days, or midnight).</p> <pre><code>TimedRotatingFileHandler(formatter, filename, when, interval, backupCount, delay, useUTC)\n</code></pre>"},{"location":"handlers/timed-rotating-file/#parameters","title":"Parameters","text":"Parameter Type Default Description <code>formatter</code> <code>table</code>|<code>nil</code> Copied from default Formatter for <code>{token}</code> substitution <code>filename</code> <code>string</code> (required) Path to the log file <code>when</code> <code>string</code>|<code>nil</code> <code>\"D\"</code> Interval type (see table below) <code>interval</code> <code>number</code>|<code>nil</code> <code>1</code> Number of <code>when</code> units between rotations <code>backupCount</code> <code>number</code>|<code>nil</code> <code>0</code> Number of old log files to keep (0 = unlimited) <code>delay</code> <code>boolean</code>|<code>nil</code> <code>false</code> (see notes  doesn't work as expected) <code>useUTC</code> <code>boolean</code>|<code>nil</code> <code>false</code> Use UTC instead of local time"},{"location":"handlers/timed-rotating-file/#when-values","title":"<code>when</code> Values","text":"Value Unit Notes <code>\"S\"</code> Seconds 1000 ms intervals <code>\"M\"</code> Minutes 60 000 ms intervals <code>\"H\"</code> Hours 3 600 000 ms intervals <code>\"D\"</code> Days 86 400 000 ms intervals <code>\"midnight\"</code> Daily at midnight Rotates at the next 00:00 local time"},{"location":"handlers/timed-rotating-file/#rotation-scheme","title":"Rotation Scheme","text":"<p>When <code>os.epoch(\"local\") &gt;= rollover_at</code>:</p> <ol> <li>Close the current file.</li> <li>Move <code>filename</code> \u2192 <code>filename.YYYY-MM-DD_HH-MM-SS</code>.</li> <li>If <code>backupCount &gt; 0</code>, delete the oldest matching backups so that at most    <code>backupCount</code> rotated files remain.</li> <li>Compute the next rollover time.</li> </ol>"},{"location":"handlers/timed-rotating-file/#example-rotate-at-midnight-keep-7-backups","title":"Example: Rotate at Midnight, Keep 7 Backups","text":"<pre><code>local logger = require(\"logger.lua\")\n\nlocal fmt = logger.Formatter(\"{asctime} [{level}] {message}\")\nlocal h = logger.TimedRotatingFileHandler(fmt, \"server.log\", \"midnight\", 1, 7)\nlocal log = logger.new(\"server\", true)\nlog:addHandler(h)\n\nlog:info(\"Server started -- will rotate at midnight\")\n</code></pre> <p>After a week you'd see:</p> <pre><code>server.log\nserver.log.2026-07-07_00-00-00\nserver.log.2026-07-06_00-00-00\nserver.log.2026-07-05_00-00-00\n...\nserver.log.2026-06-30_00-00-00   \u2190 8th oldest, deleted\n</code></pre>"},{"location":"handlers/timed-rotating-file/#example-rotate-every-30-minutes","title":"Example: Rotate Every 30 Minutes","text":"<pre><code>local h = logger.TimedRotatingFileHandler(nil, \"quicklog.log\", \"M\", 30, 10)\nlocal log = logger.new(\"monitor\", true)\nlog:addHandler(h)\n\nlog:info(\"Monitoring temp every 30-min cycle\")\n</code></pre> <p>Rollover filenames look like <code>quicklog.log.2026-07-06_14-30-00</code>.</p>"},{"location":"handlers/timed-rotating-file/#known-issues","title":"Known Issues","text":"<p><code>delay</code> parameter has no effect</p> <p>In the current source, <code>TimedRotatingFileHandler</code> always opens the file on every <code>:handle()</code> call regardless of the <code>delay</code> flag.  See Known Quirks.</p> <p>Backup filenames include timestamps</p> <p>Unlike <code>RotatingFileHandler</code> (which uses <code>.1</code>, <code>.2</code>, ...), this handler appends a human-readable timestamp to the filename. The backup matching pattern is <code>base..+</code> (filename followed by a dot and anything), so other files with dots in the same directory could be caught by the cleanup logic.</p>"},{"location":"handlers/timed-rotating-file/#related","title":"Related","text":"<ul> <li>RotatingFileHandler  size-based rotation</li> <li>FileHandler  plain file writing</li> </ul>"},{"location":"handlers/websocket/","title":"WebsocketHandler / RawWebsocketHandler","text":"<p>Send log messages to a remote server over a ComputerCraft websocket.</p>"},{"location":"handlers/websocket/#websockethandler","title":"WebsocketHandler","text":"<p>Sends the formatted string over the websocket.</p> <pre><code>WebsocketHandler(formatter, websocket)\n</code></pre> Parameter Type Default Description <code>formatter</code> <code>table</code>|<code>nil</code> Copied from default Formatter for <code>{token}</code> substitution <code>websocket</code> <code>table</code> (required) An open websocket from <code>http.websocket()</code>"},{"location":"handlers/websocket/#rawwebsockethandler","title":"RawWebsocketHandler","text":"<p>Sends a JSON-serialized table <code>[message, extra]</code> over the websocket. No formatter is applied.</p> <pre><code>RawWebsocketHandler(formatter, websocket)\n</code></pre> Parameter Type Description <code>formatter</code> <code>any</code> Ignored (the constructor accepts it but never uses it) <code>websocket</code> <code>table</code> An open websocket from <code>http.websocket()</code>"},{"location":"handlers/websocket/#example-both-handlers-side-by-side","title":"Example: Both Handlers Side by Side","text":"<pre><code>local logger = require(\"logger.lua\")\n\n-- Establish a websocket connection\nlocal ws, err = http.websocket(\"ws://logserver.example.com/ingest\")\nif not ws then\n    error(\"Websocket failed: \" .. err)\nend\n\nlocal log = logger.new(\"client\", true)\n\n-- Formatted (human-readable) feed\nlocal wsHandler = logger.WebsocketHandler(nil, ws)\nlog:addHandler(wsHandler)\n\n-- Raw JSON feed for programmatic parsing\nlocal rawHandler = logger.RawWebsocketHandler(nil, ws)\nlog:addHandler(rawHandler)\n\nlog:info(\"Client connected\", { computerID = os.getComputerID() })\n</code></pre> <p>On the server side, you would receive two messages for each log call:</p> <pre><code># From WebsocketHandler:\n[client] 2026/07/06 14:30:01 [INFO] Client connected\n\n# From RawWebsocketHandler:\n{\"Computer 1234\",\"Client connected\"}\n</code></pre> <p>Raw handler JSON format</p> <p><code>RawWebsocketHandler</code> sends <code>textutils.serializeJSON({log, extra})</code>, producing an array <code>[message, extra_object]</code>. The <code>extra</code> table includes the auto-populated keys (<code>message</code>, <code>level</code>, <code>loggername</code>, <code>asctime</code>) plus any custom keys you added.</p>"},{"location":"handlers/websocket/#notes","title":"Notes","text":"<ul> <li>Neither handler validates that the <code>websocket</code> argument is a real websocket object.   A bad object will error inside <code>:handle()</code>.</li> <li>The websocket must already be open  the library does not manage connection lifecycle.</li> </ul>"},{"location":"handlers/websocket/#related","title":"Related","text":"<ul> <li>ModemHandler / RawModemHandler  network logging over rednet</li> </ul>"}]}